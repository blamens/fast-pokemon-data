import isInArray from '../pokemon-weakness/utils/is-in-array';
import { module, test } from 'qunit';

module('Unit | Utility | is in array');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isInArray();
  assert.ok(result);
});
