import weaknessCalc from 'pokemon-weakness/utils/weakness-calc';
import { module, test } from 'qunit';

module('Unit | Utility | weakness calc');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = weaknessCalc();
  assert.ok(result);
});
